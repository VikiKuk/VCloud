# VCloud Storage

Дипломный проект: **облачное хранилище файлов** с backend на Django (REST API) и frontend на React.

Проект состоит из двух независимых частей:
- **Backend** — API, авторизация, работа с файлами и пользователями
- **Frontend** — пользовательский интерфейс (SPA)

---

## 📁 Структура проекта
VCloud/
backend/    # Django + DRF
frontend/   # React + Webpack

---

## 🛠️ Технологии

### Backend
- Python 3.10+
- Django 4.2
- Django REST Framework
- PostgreSQL
- Session-based authentication (cookies)
- django-cors-headers

### Frontend
- Node.js **>= 18**
- React **>= 18**
- Webpack **>= 5**
- Redux Toolkit
- React Router
- Yarn

---

## 🔧 Backend

### Описание

Backend реализует REST API для:
- регистрации и авторизации пользователей
- управления файлами
- администрирования пользователей

Используется кастомная модель пользователя с логином вместо username.

---

### ⚙️ Настройка окружения

Переменные окружения (`backend/.env`)
Пример (`.env.example`):

```env
DJANGO_SECRET_KEY=dev-secret-change-me
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

DB_NAME=cloud_storage
DB_USER=cloud_user
DB_PASSWORD=your_password_here
DB_HOST=127.0.0.1
DB_PORT=5432
```


### Инициализация базы данных

База данных PostgreSQL должна быть создана заранее.
После этого необходимо выполнить:
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

python manage.py migrate

После применения миграций:
- автоматически создаются все таблицы
- автоматически создаётся администратор

Данные администратора задаются через переменные окружения
или могут быть изменены вручную в базе данных / Django admin.

### ▶️ Запуск backend

cd backend
source venv/bin/activate
python manage.py runserver

Backend будет доступен по адресу: http://127.0.0.1:8000

### 🔐 Авторизация

Используется session-based authentication:
- cookies
- CSRF-токены
- CORS настроен для frontend dev-сервера

Основные эндпоинты:

- `POST /api/auth/register`
- `POST /api/auth/login`
- `POST /api/auth/logout`
- `GET  /api/auth/me`
- `GET  /api/files/`
- `POST /api/files/upload`
- `DELETE /api/files/{id}`


## 🎨 Frontend

Описание

Frontend — SPA-приложение на React, взаимодействующее с backend API.

Реализовано:
- регистрация и вход
- работа с файлами
- роли пользователей (user / admin)
- админ-панель для управления пользователями

---

### 📦 Установка

cd frontend
yarn install

▶️ Запуск в режиме разработки

Backend должен быть запущен отдельно.

yarn start
Frontend будет доступен по адресу: http://localhost:3000

Webpack Dev Server проксирует запросы /api/* и /media/* на backend.

---

### 📁 Функциональность

Пользователь
- регистрация и вход
- загрузка файлов
- просмотр списка файлов
- удаление и переименование файлов
- добавление комментариев
- получение публичных ссылок

Администратор
- просмотр списка пользователей
- удаление пользователей
- выдача прав администратора
- просмотр файлов конкретного пользователя

---

### 🏗️ Production-сборка frontend

yarn build

Собранные файлы помещаются в:
backend/static/frontend
и могут быть отданы Django как SPA.


### 📌 Примечания
Каталоги venv, node_modules, media не хранятся в репозитории
Файл .env не коммитится, используется .env.example
Frontend и backend запускаются независимо
Для разработки используется упрощённая CORS-конфигурация